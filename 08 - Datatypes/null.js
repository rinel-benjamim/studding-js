// O valor especial null não pertence a nenhum tipo de dado específico. 
// Ele é usado para indicar a ausência de valor ou a falta de um objeto.

let nulo = null;
console.log(nulo); // null

// Corresponde ao valor nulo ou vazio, e é frequentemente usado para indicar que uma 
// variável não tem um valor atribuído ou que vamos passar a usar ou que um objeto não existe.